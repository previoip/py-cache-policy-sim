18a19,24
> # ======= PATCH NOTE =======
> # ========= GLOBAL =========
> # override class config ref into
> # weakref form from parent class
> # ===== PATCH NOTE END =====
> 
84,91c90,103
<         self.epochs = config['epochs']
<         self.lr = config['lr']
<         self.topk = config['topk']
<         self.user_num = config['user_num']
<         self.item_num = config['item_num']
< 
<         # get this matrix from utils.get_inter_matrix and add it in config
<         self.interaction_matrix = config['inter_matrix']
---
>         self.epochs = self.wref_config['epochs']
>         self.lr = self.wref_config['lr']
>         self.topk = self.wref_config['topk']
>         self.user_num = self.wref_config['user_num']
>         self.item_num = self.wref_config['item_num']
> 
>         # get this matrix from utils.get_inter_matrix and add it in self.wref_config
> 
>         # ======= PATCH NOTE =======
>         # defer item retrieval
>         # self.interaction_matrix = self.wref_config['inter_matrix']
>         self._get_interaction_matrix = lambda: self.wref_config['inter_matrix']
>         self.interaction_matrix = None
>         # ===== PATCH NOTE END =====
93,94c105,106
<         self.embedding_size = config['factors']
<         self.hidden_size_list = config["hidden_size_list"] if config['hidden_size_list'] is not None else [64, 64, 64]
---
>         self.embedding_size = self.wref_config['factors']
>         self.hidden_size_list = self.wref_config["hidden_size_list"] if self.wref_config['hidden_size_list'] is not None else [64, 64, 64]
97,100c109,112
<         self.node_dropout = config['node_dropout']
<         self.message_dropout = config['mess_dropout']
<         self.reg_1 = config['reg_1']
<         self.reg_2 = config['reg_2']
---
>         self.node_dropout = self.wref_config['node_dropout']
>         self.message_dropout = self.wref_config['mess_dropout']
>         self.reg_1 = self.wref_config['reg_1']
>         self.reg_2 = self.wref_config['reg_2']
113,116c125,128
<         self.loss_type = config['loss_type']
<         self.optimizer = config['optimizer'] if config['optimizer'] != 'default' else 'adam'
<         self.initializer = config['init_method'] if config['init_method'] != 'default' else 'xavier_normal'
<         self.early_stop = config['early_stop']
---
>         self.loss_type = self.wref_config['loss_type']
>         self.optimizer = self.wref_config['optimizer'] if self.wref_config['optimizer'] != 'default' else 'adam'
>         self.initializer = self.wref_config['init_method'] if self.wref_config['init_method'] != 'default' else 'xavier_normal'
>         self.early_stop = self.wref_config['early_stop']
121c133,139
<         self.norm_adj_matrix = self.get_norm_adj_mat().to(self.device)
---
>         # ======= PATCH NOTE =======
>         # defer item retrieval
>         # self.norm_adj_matrix = self.get_norm_adj_mat().to(self.device)
>         self._get_norm_adj_matrix = lambda: self.get_norm_adj_mat().to(self.device)
>         self.norm_adj_matrix = None
>         # ===== PATCH NOTE END =====
> 
125a144,150
> 
>         # ======= PATCH NOTE =======
>         # retrieve deferred item
>         if self.interaction_matrix is None:
>             self.interaction_matrix = self._get_interaction_matrix()
>         # ===== PATCH NOTE END =====
> 
158a184,190
> 
>         # ======= PATCH NOTE =======
>         # retrieve deferred item
>         if self.norm_adj_matrix is None:
>             self.norm_adj_matrix = self._get_norm_adj_matrix()
>         # ===== PATCH NOTE END =====
> 
